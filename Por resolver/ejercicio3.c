/*
Dado un arreglo de 10 numeros enteros positivos diferentes, se pide:
	a- Cargar el arreglo en etapa de declaracion.
	b- Generar un nuevo vector o arreglo, con aquellos numeros que son multiplos de 3 y otro con el resto de los elementos del vector original.
	Si el arreglo que almacena los num multiplos de 3 tiene elementos, se pide
		1) ordenar los elementos de menor a mayor. Usar funcion para ordenar
		2) completar el arreglo ordenado con multiplos de 3, de tal forma que mantenga la condicion de ordenado (usar funcion o procedimiento para completar)
		3) indicar cuantos elementos fueron agregados
	c- Mostrar los 3 vectores. Usar procedimiento.
*/

#include <stdio.h>

int main(int argc, char *argv[]) {
	
 	turn 0;
}

